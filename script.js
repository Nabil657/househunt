document.addEventListener("DOMContentLoaded", function () {
    fetchPosts();

    function fetchPosts() {
        // ডাটাবেস থেকে তথ্য লোড করার জন্য এখানে API কল করা হবে
        console.log("Loading posts...");
    }
});